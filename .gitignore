# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
dist/
build/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
coverage/
.coverage
# Node / TypeScript
node_modules/
.next/
out/
cdk.out/
*.tsbuildinfo
.turbo/
npm-debug.log*
# Env / secrets
.env*
!.env.example
!**/.env.example
*.key
*.crt
secrets/
# Universal
.DS_Store
Thumbs.db
*.tmp
*.swp
.vscode/
.idea/
*.log
# Kakuto landing page — the 7.2MB self-contained source bundle is the regen source for the
# deployable index.html + assets/ (via LP/unpack_bundle.py); keep the bundle out of git history.
LP/Kakuto LP (standalone).html
# Agent skills installed via `npx skills` (manifest: skills-lock.json; restore: npx skills experimental_install).
# The hand-written review skills (review-map/boundary-audit/tenant-audit) are committed normally; only the
# npx-materialised symlinks and their .agents/ store are ignored.
/.agents/
/.claude/skills/frontend-design
/.claude/skills/baseline-ui
/.claude/skills/fixing-accessibility
/.claude/skills/fixing-motion-performance
/.claude/skills/grill-me
