[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "raku-rag-sdk"
version = "0.1.0"
description = "Python SDK for the raku-rag HTTP API"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["raku_rag_sdk*"]
