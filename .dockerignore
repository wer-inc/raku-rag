.git/
node_modules/
**/node_modules/
**/dist/
**/build/
**/.next/
**/out/
__pycache__/
**/__pycache__/
*.pyc
.venv/
venv/
.env*
!**/.env.example
*.log
coverage/
.mypy_cache/
.ruff_cache/
.pytest_cache/
# CDK Docker-image assets use the repo ROOT as build context (ecs.ContainerImage.fromAsset(REPO_ROOT));
# exclude only the synth output so the asset staging doesn't recursively self-copy (ENAMETOOLONG).
# NOTE: do NOT exclude specs/ — apps/web's `next build` reads specs/full-saas/screens.manifest.json.
**/cdk.out/
cdk.out/
